apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Frolo'
            keyPassword 'j15avaCkurt7'
            storeFile file('../frolojks.jks')
            storePassword 'j15avaCkurt7'
        }
    }

    compileSdkVersion 28
    defaultConfig {
        applicationId "com.froloapp.telegramchart"
        minSdkVersion 15
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        debug {
            minifyEnabled false
            resValue "string", "app_name", "Telegram Chart Dev"
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            versionNameSuffix "-D"
            applicationIdSuffix ".dev"
        }
        release {
            resValue "string", "app_name", "Telegram Chart"
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            versionNameSuffix "-R"
            signingConfig signingConfigs.config
        }
    }

}

dependencies {
    // make it as independent as possible
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    testImplementation 'junit:junit:4.12'
}
